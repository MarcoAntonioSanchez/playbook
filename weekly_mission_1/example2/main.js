// node main.js

const logger = require('./logger') // Importando el módulo anteriormente exportada llamado logger y almacenandolo en una variable del mismo nombre para disponer del código dentro de este archivo

logger.info('This is an informational message') // Asignando el mensaje que contendrá el parámetro message dentro del archivo logger
logger.verbose('This is a verbose message') // Asignando el mensaje que contendrá el parámetro emssage también pero dentro del valor verbose
